# Angular Frontend (minimal) for OrderSystemApi

## Setup
1. cd frontend (this folder)
2. npm install
3. npm start
4. Open http://localhost:4200

## Notes
- API base URL is configured in src/app/services/api.service.ts (default: http://localhost:5000)
- This is a minimal frontend for quick testing and not production-ready.
